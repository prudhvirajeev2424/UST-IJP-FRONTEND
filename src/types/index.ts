// Shared types

export type Profile = {
  id: string;
  name: string;
  position: string;
  uid: string;
  soId: string;
  status: string;
  fitment: number;
  skills: string[];
  avatar?: string;
};

export default {} as unknown as {};
