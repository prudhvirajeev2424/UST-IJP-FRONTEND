// Shared types

export type Profile = {
  id: string;
  name: string;
  developer?: string;
  uid?: string;
  score: number;
  status?: string;
  skills: string[];
  avatar?: string;
};

export default {} as unknown as {};
